# Copy to .env for local dev; set these as build-time env vars in the
# Cloudflare Pages project settings for deployments.

# Base URL of the FreeDomains backend API.
VITE_API_URL=http://localhost:5000

# Cloudflare Turnstile site key (public).
VITE_TURNSTILE_SITE_KEY=
